# Templates

This directory contains lightweight starter templates for recurring repository artifacts.

Use these templates to keep new files structurally consistent without forcing heavy process too early.

Current templates:

- `WORKFLOW.md`

Adapt each template to the actual use case instead of filling every section mechanically.

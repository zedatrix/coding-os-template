# Copy this file to `.env` and fill in only the values needed locally.
# Do not commit real credentials.

# ClickUp
CLICKUP_API_TOKEN=

# Linear (generate at Linear → Settings → API → Personal API keys)
LINEAR_API_KEY=

# Add additional integration variables below as they are introduced.
